/**
 * Terrain types used only in Legends of Valor.
 * This does NOT replace TileType; it is an additional classification
 * that lets us keep the original Monsters & Heroes code intact.
 */
public enum TerrainType {
    HERO_NEXUS,
    MONSTER_NEXUS,
    INACCESSIBLE,
    OBSTACLE,
    PLAIN,
    BUSH,
    CAVE,
    KOULOU
}
